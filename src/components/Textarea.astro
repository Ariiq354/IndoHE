---
const { title, row, white } = Astro.props;
---

<div class="flex flex-col">
  <h2 class={`${white ? "text-white" : ""}`}>{title}</h2>
  <textarea
    rows=`${row}`
    class="rounded-md border border-black/40 bg-white p-2"
    aria-label={title}></textarea>
</div>
