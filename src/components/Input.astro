---
const { title, white } = Astro.props;
---

<div class="flex flex-col">
  <h2 class={`${white ? "text-white" : ""}`}>{title}</h2>
  <input type="text" class="rounded-md border border-black/40 bg-white p-2" />
</div>
